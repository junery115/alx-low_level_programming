#include "main.h"

/**
 * _isupper - functions that test a character if its uppercase or not
 * @i: variable to be tested
 * Return: 0
 */

int _isupper(int i)
{
	if (i >= 65 && i <= 90)
	{
		return (1);
	}
	else
	{
		return (1);
	}
}
