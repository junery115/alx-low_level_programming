#include "main.h"
/**
 * add -  adds 2 numbers
 * Description: adds 2 numbers and returns results
 * @a: first parameter
 * @b: second parameter
 * Return:  results
 */

int add(int a, int b)
{
	return (a + b);
}
