#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: uses printf to print "with proper grammar,
 * but the outcome is a piece of art,"
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("with proper grammar, but the outcome is a piece of art,");
return (0);
}
